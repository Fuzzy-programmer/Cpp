#include<bits/stdc++.h>
using namespace std;
// unordered set - not sorted but unique| all operations are O(1)
int main(){
    // fstream f;
    // f.open("a.out",ios::in);
    // char ch;
    // while(!f.eof()){
    //     cout<<ch;
    //     ch=f.get();
    // }
    // cout<<endl;
    // f.close();

    unordered_set<int> us;
    // lower_bound and upper_bound functions does not works, rest all functions are as set, 
    // It does not store in any particular order it has better complexity than set in most cases, 
    // except some when collision happens O(n).
}